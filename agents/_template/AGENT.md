# Agent 角色定义模板

> 使用本模板创建新的 Agent 角色定义。将占位符替换为实际内容后，保存到 `agents/<role>/AGENT.md`。

---

## 角色信息

- **角色名称**：`<角色中文名>`
- **角色标识**：`<role-id>`（对应 workflow YAML 中的 agent 字段）
- **一句话定位**：`<该角色在团队中的核心职责>`

---

## 职责范围

### 负责的工作流阶段

| 阶段 ID | 阶段名称 | 主要产物 |
|---------|---------|---------|
| `<stage_id>` | `<阶段名称>` | `<产物文件名>` |

### 核心职责

1. `<职责 1>`
2. `<职责 2>`
3. `<职责 3>`

### 不属于本角色的职责

- `<明确排除的职责，避免越权>`

---

## 可用技能

| 技能 | 路径 | 用途 |
|-----|------|------|
| `<技能名>` | `skills/<skill-id>/SKILL.md` | `<简要说明>` |

---

## 工作规范

### 输入要求

- 在开始工作前，必须读取以下上游产物：
  - `<上游产物路径及说明>`

### 输出标准

- 产物必须基于 `workflow/_templates/<template>.md` 模板创建
- 必须满足工作流定义中的 `validation` 校验规则
- `<其他输出标准>`

### 协作约定

- `<与上游角色的协作方式>`
- `<与下游角色的协作方式>`

---

## 行为约束

1. 严格在角色职责范围内操作，不修改其他角色的产物
2. 所有决策和判断必须记录在产物文档中
3. 遇到上游产物不清晰时，在产物中标注 `[待确认]` 而非猜测
4. 产物完成后必须调用 `validate-artifact` 技能进行质量校验
