# 产品需求文档（PRD）

> **需求编号**：FEAT-xxx  
> **需求名称**：`<需求名称>`  
> **创建日期**：`<YYYY-MM-DD>`  
> **负责人**：`<PM 姓名>`  
> **状态**：草稿 / 评审中 / 已定稿

---

## overview | 需求概述

### 背景

`<描述需求产生的背景和业务场景，为什么要做这个功能>`

### 目标

`<明确本需求要达成的核心目标，可量化的业务指标优先>`

### 范围

- **包含**：`<本次需求包含的功能范围>`
- **不包含**：`<明确排除在外的功能，避免范围蔓延>`

---

## user_stories | 用户故事

### 故事 1：`<故事标题>`

- **作为** `<用户角色>`
- **我想要** `<具体功能>`
- **以便** `<获得的价值>`

**补充说明**：`<该故事的额外上下文或约束>`

### 故事 2：`<故事标题>`

- **作为** `<用户角色>`
- **我想要** `<具体功能>`
- **以便** `<获得的价值>`

> 按需添加更多用户故事。每个独立的用户行为路径应有对应的故事。

---

## acceptance_criteria | 验收标准

### AC-1：`<验收标准标题>`

- **前置条件**：`<触发该场景的前提条件>`
- **操作**：`<用户执行的操作>`
- **预期结果**：`<可验证的结果描述>`

### AC-2：`<验收标准标题>`

- **前置条件**：`<触发该场景的前提条件>`
- **操作**：`<用户执行的操作>`
- **预期结果**：`<可验证的结果描述>`

> 每个用户故事至少对应一条验收标准。验收标准必须可量化、可验证。

---

## 补充信息

### 非功能性需求

- **性能**：`<性能要求，如响应时间、并发量>`
- **安全**：`<安全要求，如数据加密、权限控制>`
- **兼容性**：`<兼容性要求，如浏览器、设备>`

### 依赖与风险

| 依赖/风险 | 描述 | 影响 | 缓解措施 |
|-----------|------|------|---------|
| `<项目>` | `<描述>` | `<影响>` | `<措施>` |

### 优先级

- **紧急程度**：高 / 中 / 低
- **业务价值**：高 / 中 / 低
- **实现复杂度**：高 / 中 / 低

---

## 变更记录

| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|-------|
| `<YYYY-MM-DD>` | v1.0 | 初始版本 | `<姓名>` |
