# 缺陷分析报告

> **缺陷编号**：BUG-xxx
> **关联需求**：`<FEAT-xxx>`
> **分析日期**：`<YYYY-MM-DD>`
> **分析师**：`<Developer 姓名>`
> **状态**：分析中 / 已定稿

---

## 1. 缺陷概述

### 缺陷标题

`<一句话描述缺陷现象>`

### 严重程度

- [ ] **P0 - 阻塞**：核心功能完全不可用
- [ ] **P1 - 严重**：核心功能受限，影响主要业务流程
- [ ] **P2 - 一般**：非核心功能异常，有替代方案
- [ ] **P3 - 低**：轻微异常，不影响使用

### 影响范围

`<描述缺陷影响的用户群体、业务流程、模块>`

---

## 2. 缺陷复现

### 复现环境

| 环境项 | 值 |
|--------|-----|
| 操作系统 | `<OS 和版本>` |
| 浏览器 | `<如有前端问题>` |
| 版本 | `<应用版本>` |
| 其他 | `<其他相关环境>` |

### 复现步骤

1. `<步骤1>`
2. `<步骤2>`
3. `<步骤3>`

### 预期结果

`<描述正常情况下的预期行为>`

### 实际结果

`<描述当前观察到的错误行为>`

### 复现概率

- [ ] 100% — 必现
- [ ] >50% — 经常出现
- [ ] <50% — 偶现
- [ ] 难以复现

---

## 3. 根因分析

### 初步分析

`<基于代码审查、日志分析、调试得出的初步判断>`

### 根本原因

```
<用代码片段、日志、调用链等方式展示根本原因>
```

### 影响路径

```
<用文字或图表描述缺陷的完整影响路径>
例：
用户操作 → Controller 接收请求 → Service 调用 → Repository 查询 →
SQL 执行 → 数据库返回 → 序列化 → 响应
```

### 定位依据

| 依据 | 内容 |
|------|------|
| 日志 | `<相关错误日志>` |
| 代码 | `<相关代码位置>` |
| 数据库 | `<相关 SQL 或数据问题>` |

---

## 4. 修复方案

### 方案描述

`<描述修复的技术方案>`

### 修复范围

- **修改文件**：
  - `<file1>`
  - `<file2>`
- **新增文件**：
  - `<file3>`
- **删除文件**：
  - `<file4>`

### 数据迁移（如有）

`<如有数据库变更，描述数据迁移方案>`

```
-- 迁移 SQL
```

### 回滚方案

`<描述如何回滚本次修复>`

---

## 5. 修复计划

### 工时预估

| 任务 | 预估工时 | 负责人 |
|------|---------|-------|
| `<任务>` | `<X 小时>` | `<姓名>` |

### 里程碑

| 里程碑 | 计划完成时间 |
|--------|------------|
| 代码完成 | `<日期>` |
| 测试通过 | `<日期>` |
| 发布上线 | `<日期>` |

---

## 6. 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| `<风险>` | 高/中/低 | `<影响>` | `<措施>` |

---

## 7. 变更记录

| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|-------|
| `<YYYY-MM-DD>` | v1.0 | 初始版本 | `<姓名>` |
