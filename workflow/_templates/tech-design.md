# 技术设计文档

> **需求编号**：FEAT-xxx  
> **需求名称**：`<需求名称>`  
> **创建日期**：`<YYYY-MM-DD>`  
> **负责人**：`<Developer 姓名>`  
> **关联 PRD**：`<prd.md 的相对路径>`  
> **状态**：草稿 / 评审中 / 已定稿

---

## architecture | 架构设计

### 整体方案

`<描述整体的技术方案和架构选型，说明为什么选择这个方案>`

### 架构图

```
<用文本或 mermaid 绘制架构图，展示模块关系和数据流向>
```

### 模块划分

| 模块 | 职责 | 技术选型 | 说明 |
|------|------|---------|------|
| `<模块名>` | `<模块职责>` | `<使用的技术/框架>` | `<选型理由>` |

### 设计决策

| 决策 | 方案 | 备选方案 | 选择理由 |
|------|------|---------|---------|
| `<决策点>` | `<选定方案>` | `<其他方案>` | `<为什么选这个>` |

---

## api_design | API 设计

### 接口 1：`<接口名称>`

- **方法**：`GET / POST / PUT / DELETE`
- **路径**：`/api/v1/<resource>`
- **描述**：`<接口用途>`

**请求参数**：

| 参数 | 类型 | 必填 | 描述 |
|------|------|------|------|
| `<参数名>` | `<类型>` | 是/否 | `<描述>` |

**请求体**（如适用）：

```json
{
  "<field>": "<type | description>"
}
```

**响应体**：

```json
{
  "code": 200,
  "data": {
    "<field>": "<type | description>"
  }
}
```

**错误码**：

| 错误码 | 描述 | 触发条件 |
|-------|------|---------|
| `<code>` | `<描述>` | `<触发条件>` |

> 按需添加更多接口定义。

---

## data_model | 数据模型

### 实体 1：`<实体名称>`

| 字段 | 类型 | 约束 | 描述 |
|------|------|------|------|
| `id` | `bigint` | `PK, AUTO_INCREMENT` | 主键 |
| `<字段名>` | `<类型>` | `<约束>` | `<描述>` |
| `created_at` | `datetime` | `NOT NULL` | 创建时间 |
| `updated_at` | `datetime` | `NOT NULL` | 更新时间 |

**索引**：

| 索引名 | 字段 | 类型 | 用途 |
|-------|------|------|------|
| `<索引名>` | `<字段>` | `UNIQUE / INDEX` | `<用途>` |

**实体关系**：

```
<用文本或 mermaid 绘制 ER 图>
```

> 按需添加更多实体定义。

---

## 实现计划

### 任务拆分

| 任务 | 预估工时 | 优先级 | 依赖 |
|------|---------|-------|------|
| `<任务描述>` | `<工时>` | P0/P1/P2 | `<前置任务>` |

### 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| `<风险描述>` | 高/中/低 | `<影响描述>` | `<缓解措施>` |

---

## 变更记录

| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|-------|
| `<YYYY-MM-DD>` | v1.0 | 初始版本 | `<姓名>` |
